# Прогресс: $class/ai

## Последние изменения
- [15:20] `config.js` — дефолты класса (пока `model`), наследуется через `~`; цепочка модели: `agent.model → params.model → config.model`, нет нигде — понятная ошибка. Со всех агентов сняты строгие `model` (gemma3 4b). Причина: прогон — web на слабой модели агента вместо модели задачи; дефолт нужен только для standalone без model.
- [15:20] Владелец исполнения — `params.$context` (ставит HTTP-диспетчер или таск), `this.$context` — фолбэк; `loadAgent`/`loadConfig`/`buildSystemPrompt` берут owner аргументом. Причина: прогон — падение `undefined (reading 'meta_folder')`: привязка `item.$context` разделяемая, её перебивает параллельный `_methods` другого элемента.
- [14:35] `prompt/$method` — единый движок агентов: контракт `live` (send/save/stopped/wait/mode) от владельца ленты; standalone REST — тихий live c path класса. Причина: одна реализация для one-shot и живой ленты $task.
- [14:35] Приоритет модели инвертирован: `agent.model ?? model` — строгая модель агента побеждает приехавшую. Причина: агент со строгой моделью работает только на ней.
- [14:35] `streamChat` объединён с task-версией: effort (гейт `allowReasoning`), `maxOutput`, `usage`, стоп и reasoning-блок через `live`. Причина: одна стрим-обёртка, владелец ленты — адресат событий.
- [14:35] System всегда пересобирает исполнитель (`system.md ~` + место + время `tz`); `messages` — только диалог-улики. Стоп инструмента (`tool.stop`) — `await live.wait`; стоп лист-агента возвращается владельцу. `exec` — function-call инструментов (бывший `_fc_exec`). Причина: слои контекста по владельцам, стоп на стороне агента.
- [14:35] `web.js` / `work.js` — единый контракт `init({ block, box, messages, agent, live, exec, streamChat })`, убраны развилки `task.*`; `work.finish` возвращает mode в plan. Причина: агент не знает, кто его запустил.

## В работе
- Кросс-классовый запуск агентов (адресация чужого класса, показ его агентов) — следующая итерация.

## Ключевые решения
- Решение: живость ленты — контракт `live` (null-object для REST), а не второй код-путь. Причина: один движок, персист и события принадлежат владельцу ленты.
- Решение: улики едут диалогом `messages` (handoff-проекция от заказчика), system — всегда локальный. Причина: место исполнения обязано давать свой `system.md ~`.

## Блокеры / Открытые вопросы
- Нет.
