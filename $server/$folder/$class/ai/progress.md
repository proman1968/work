# Прогресс: $class/ai

## Последние изменения
- [10:35] `export default` в начале `$method`; `params.context` — продолжить локально, иначе новый body + system из `system.md`; в ответе `context`. Причина: канон порядка файла + передаваемый контекст агента.

## В работе
- Поля `model` в паспортах агентов.

## Ключевые решения
- Решение: контекст one-shot — объект в params/ответе, не файл `.task`. Причина: агент развивает локальный журнал.

## Блокеры / Открытые вопросы
- Нет.
