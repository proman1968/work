# Прогресс: $ai / task.ai

## Последние изменения

- [01:05] Prose `propose_plan({intro, steps:[…]})` → План (прогон `1785189073584`). GigaChat после teach ask_user написал JS-style вызов с unquoted keys и массивом `steps` текстом — `parseJsObjectLiteral` падал на `[` → мёртвый text, wait. 1) `parseJsObjectLiteral` + `parseJsArrayLiteral` / `extractBalancedArray`: массивы и вложенные объекты в JS-style args. 2) `parseProposePlanTextBlock` поднимает prose-блок в `pendingPlan` до фильтра огрызков; `isBareToolProseText` ловит `name({…` если парсинг всё же упал. 3) System-промпт: textual `name({…})` — допустимый fallback; запрещены только теги/кривые формы (`<plan>`, `{subplan}[…]`, `subplan <…>`). Прогон: tests/*.test.js — 151 pass, 0 fail.
- [00:55] Вложенная регистрация subplan-задач — настоящий стек (прогон `1785187872055`). Раньше `applySubplan` пушил дочернюю задачу соседом в `body.ribbon` → две активных задачи на одном уровне, лента родителя обрывалась, модель путалась. 1) `applySubplan` пушит в `outer.ribbon` (ленту родителя). 2) `activeTaskChain` + рекурсивный `findActiveTask` — спуск к самой глубокой активной; `ribbonTargetOf`/`advanceTask` работают без правок. 3) `subplanGateError` считает глубину по цепочке; `findOpenInteractive` обходит ленты стека от глубокой к корню. 4) Do-инъекция в system через `findActiveTask`. 5) `tipBranch` превью спускается в глубину. Обратная совместимость: плоские старые файлы читаются как раньше. Прогон: tests/*.test.js — 147 pass, 0 fail.
- [00:45] Жёсткие промпты: конкретные имена функций + готовые примеры вызовов (по прогону `1785187872055` — GigaChat изобретает всё новые текстовые формы: `subplan < steps [...] >`). 1) SYSTEM_PROMPT (on_save-триггер): секция «Формат ответа» с тегами заменена на «Каналы действий — ТОЛЬКО вызовы функций»: примеры ask_user/propose_plan/subplan/save_file/complete_step/report с реальными аргументами + явный запрет псевдовызовов в тексте («subplan <…>», «{subplan}[…]» не исполняются); теги <plan>/<subplan>/<questions> из справочника убраны (парсер-fallback остался, но модель их больше не видит). 2) TYPES.thinking.servicePrompt (все роли) — примеры вызовов с аргументами + «имя функции в тексте не исполняется»; TYPES.task — пример subplan({steps:[…]}); TYPES.tool_result — примеры complete_step/save_file. 3) Все teach-тексты с готовыми примерами: ASK_USER_TEACH, planQualityError, subplanGateError (+ подстановка номера текущего шага в complete_step), evidence-ошибка complete_step, clarify-ошибка, duplicateAskTeach, makeStepInstruction (clarify- и do-ветки). Прогон: tests/*.test.js — 144 pass, 0 fail.
- [00:30] Prose-subplan «{subplan}[…]» + ворота качества (прогон `1785186415804`). Шаг 1 (clarify-опрос) закрылся штатно; на шаге 2 после teach «повтор шагов» модель написала `{subplan}[{description}]` прозой — не `<subplan>` и не FC → мёртвый text, автомат встал. 1) `consumeBraceSubplan` в `parseResponseToRibbon` (формы `{subplan}[…]` / `{subplan}{…}`) + страховка `parseSubplanTextBlock` в prompt() до фильтра огрызков — prose-форма поднимается в `pendingSubplan`. 2) `subplanGateError`: <2 осмысленных подшагов → teach «декомпозиция — минимум 2 подшага» (один перефраз текущего шага больше не создаёт вложенную задачу). 3) `isBareToolProseText` ловит битые `{subplan}[`-огрызки — при сбое JSON поток не глохнет, драйвером остаётся teach. Прогон: tests/*.test.js — 144 pass, 0 fail.
- [00:05] Огрызок «ask_user» глушил ретрай + label задачи (прогон `1785185612094`). Ворота прошлой итерации сработали (subplan-повтор, plan-teach, дубли — все отбиты), но модель вызвала `ask_user({…})` с открытыми вопросами: ворота tap-first убрали опрос и запушили teach, а огрызок прозы `ask_user` из того же ответа осел text-блоком после teach-`tool_result` → text = wait, ретрай не ушёл. Фильтр голого ask_user стоял только в ветке «FC-вызова нет». 1) `isBareToolProseText` (голое имя канала ask_user/propose_plan/subplan/complete_step/save_file/report/search или сломанная ask_user-форма) + безусловный фильтр таких text-блоков — драйвером остаётся teach, ретрай уходит, повторный ask_user проходит по `hasRecentAskOptionsTeach` как textarea. 2) Label задачи — первая строка плана без маркера списка («- собрать…» → «собрать…»). Прогон: tests/*.test.js — 139 pass, 0 fail.
- [23:55] Wait-порядок, дубль-опросы, subplan-ворота (прогон `1785184209966`). 1) Teach-`tool_result` (planTeach/askTeach) пушатся ДО блоков + `hoistWaitBlock`: незакрытый интерактив хода всегда хвост ленты — иначе драйвером становился tool_result, рекурсия ехала дальше, опрос повисал (KPI-кейс: ask_user+propose_plan в одном ходе). 2) Дубль-опрос ищется по всему стеку задач (`collectAnsweredInteractive`): свежая subplan-задача с пустой лентой больше не переспрашивает тему/цель. 3) `subplanGateError`: subplan-копия шагов родителя и глубина стека ≥3 → обучающий отказ (в прогоне было 4 уровня задач с дублем плана); при конверсии План→subplan вычищается мёртвая кнопка action «План». 4) Эхо `[инструкция]…` срезается в `normalizeModelToolProse`; проза-обвязка рядом с опросом (`foldProseIntoInteractive`) → в `questions.content`, boilerplate «Сформулирую…» отбрасывается. 5) `подагент=ask_user({…` (префикс + несбалансированные скобки) → `ASK_USER_TEACH`; голый JSON-массив шагов в прозе → `parseJsonStepsArray` → pendingPlan (ретрай плана через ворота качества). Восстановлены именованные экспорты для тестов; `tests/prompt-pipeline.test.js` (16 кейсов). Прогон: tests/*.test.js — 138 pass, 0 fail (fc-args и role-acl ожили с экспортами).
- [23:20] `prompt` = линейный пайплайн на servicePrompt. Тело метода — 10 шагов: вход → messages (к пользовательскому промпту всегда плюсуется служебный «думай») → реальный промпт в ribbon → модель → реальный ход = thinking целиком, ASSISTENT = парсинг/FC → push → save → рекурсия. Правило рекурсии одно: `svc = dynamicFollow || resolveServicePrompt(последний блок, injRole)`; есть — `this.prompt({role:'ASSISTENT', prompt: svc})`, нет — wait. Служебный промпт резолвится в момент push и передаётся ПАРАМЕТРОМ (`params.prompt`), не пересчитывается на входе следующего хода. Убраны `thinkPhase`/`driverEntry`-инъекция на входе/`followInstruction`-плюминг/`WAIT_USER_TYPES`; вход пользователя (confirm/answers/retry/pendingAction) — `applyUserInput` → блоки + dynamicFollow; нераспознанный ответ ASSISTENT — text «Требуется уточнение…» (wait, не мёртвый цикл); `driverEntry` пропускает отвеченный интерактив. Ворота (ACL, teach, evidence, plan quality, pendingAction) сохранены внутри этапа парсинга.
- [22:12] `<propose_plan>` YAML → План (прогон `1785179372057`). GigaChat писал `<propose_plan>` с `intro:`/`steps:`/`- …` вместо FC/`<plan>` → мёртвый text. 1) `parsePlanBodySteps` — JSON + numbered + bullet. 2) `consumePlanXmlTag` — `<plan>` и `<propose_plan>` → pendingPlan / planRejected. 3) Стрип `propose_plan` в сырых тегах.
- [22:05] GigaChat `<|superquote|>` + `ask_user{…}` (прогон `1785179002909`). После teach про options модель писала prose `ask_user{ <|superquote|>fields…}` без `()` → парсер не собирал call, мёртвый `text`. 1) `normalizeModelToolProse` — `<|superquote|>`→`"`. 2) `parseJsStyleToolCallAt` / поиск / strip — форма `name{…}` без скобок. 3) Teach-fallback, если `ask_user{`/`(` не разобрался.
- [21:59] Clarify-ворота + мёртвый ask_user (прогон `1785178185278`). Шаг «Запросить название…» не матчился `stepNeedsClarify` (`спрос`≠`запрос`) → Do/save_file вместо PROPOSE/ask_user; модель писала `<tool>ask_user` → блок `text` и стоп без опроса. 1) В `stepNeedsClarify` основа `запрос`. 2) Стрип незакрытого `<tool>name`; сырой канал ask_user → `ASK_USER_TEACH` / tool_result (ретрай), не wait на text.
- [18:55] Think-фаза без тегов + каналы только через function calling (разбор прогона `1785165925951` — GigaChat Light подумал без `<reasoning>`, парсер записал text, wait-состояние убило автомат). Принцип: тип блока задаёт харнесс по фазе, а не разметка модели. 1) Think-ход (`thinkPhase` = реальный prompt-драйвер или ASSISTENT-текст шага): functions в запрос **не передаются** (tool физически невозможен), весь ответ целиком → блок `thinking` (`stripReasoningWrapper` снимает случайные обёртки); парсер каналов на этом ходе не вызывается — классификации нет по построению. 2) Action-ход: инструкции — технические команды с точными именами функций («вызови функцию ask_user({questions})», propose_plan, save_file, complete_step, search); либо ответ обычным текстом, либо один FC-вызов; XML-теги из servicePrompt удалены (остались только как толерантный fallback парсера). 3) Новый FC-канал `report({content})` — финальный отчёт: text-блок + action «Отчёт/Принять» строит харнесс; детерминированный fallback — если все пункты done и ответ text, кнопку «Принять» дорисовывает харнесс сам (модель не просят рисовать `<action>`).
- [18:25] servicePrompt = команда «двигайся дальше»: у wait-состояний (`text`, `action`, `form`, `questions` — `WAIT_USER_TYPES`) servicePrompt удалён — автомат в них остановлен, следующего хода модели нет, команда движения там противоречие. servicePrompt остаётся только у состояний-продолжений: `prompt` (думай), `thinking` (одно действие), `tool_result`, `tool`, `task`, `file`, `error`. `resolveServicePrompt` без текста даёт `''` — инъекции нет; остановку обеспечивает `WAIT_USER_TYPES`, не тексты.
- [17:45] Канон промптов: реальные vs инъекции, различение по роли. 1) Два вида промптов: реальный (role `USER|BOSS|ADMIN`, всегда приходит с клиента, default USER) → блок `prompt` в ленту; служебный (role `ASSISTENT` — самовызовы шагов) → **только на острие** messages текущего вызова, в ленту и историю следующих ходов не попадает. 2) Автомат «одно действие за ход»: после реального промпта инъекция `TYPES.prompt.servicePrompt` = «только думай»; блок `thinking` фиксируется в ленту; следующий ход — развилка `TYPES.thinking.servicePrompt` (ответ text | ask_user | propose_plan | исполнение пункта). 3) `servicePrompt` — строка или объект ролевых вариантов `{default, USER, BOSS, ADMIN, ASSISTENT}`; `driverDirective` и `ROLE_OVERLAYS` схлопнуты в TYPES (один источник правды). 4) Шаги плана: step-prompt'ы `sender:'WORK'` из лент удалены; после «Начать»/`complete_step`/subplan — `this.async(() => this.prompt({role:'ASSISTENT', text:'Делай пункт N плана…'}))`; финальный Отчёт — той же инъекцией. `stepEvidence` — по `step.startedAt` (span = блоки ленты после старта шага), не по step-prompt. 5) `buildHistoryFromRibbon` — только факты диалога: вырезаны `inServiceScope`/`appendServicePrompt`/вклейка `[инструкция]` в прошлые сообщения (раньше модель получала пачку устаревших протоколов в «истории»). 6) `promptTurn`-обёртка влита в `prompt` (this = task.ai); named-export для тестов удалён (в DATA попадает только default); удалён мёртвый `methods/prompt/$method` (импортировал несуществующий `ai-prompt/legacy.js`); тесты-фиксаторы старых внутренностей (prompt-pipeline, plan-steps, interactive-types, role-acl, fc-args, context-bundle, mvp-e2e-roles, create-class-only) удалены — контракт проверяет `import-script.test.js` (merge-цепочка, default export). 7) Клиент всегда шлёт `role`: preview (`_userRole()`, default USER) и on_save-триггер. Прогон: tests/*.test.js — 90 pass; `tests/class/distributed-folder` падал до правок (не связан).

## В работе

- Обкатка thinking-first автомата на живых прогонах (следить: модели, игнорирующие «только думай»).
- Фаза 5: trust markings UI + plan→diff→apply для `$class`/handlers.
- Параллельные subagents + merge.

## Ключевые решения

- **Роль — дискриминатор промпта.** Реальный (USER/BOSS/ADMIN, с клиента) → лента; ASSISTENT (система) → только острие.
- **Инъекции ephemeral.** `TYPES[состояние].servicePrompt` по паре (тип последнего блока, роль); в JSON задачи не хранятся, в историю не накапливаются.
- **prompt = метод файла.** Один проход; продолжение — `this.async(() => this.prompt({role:'ASSISTENT', …}))`.
- **system = identity; ход = одна инъекция.** Думай → один канал.
- **body.usage = истина по задаче.** Все LLM-ходы.
- **Фаза = данные файла.** `task` в ribbon, статусы шагов + `startedAt`.

## Блокеры / Открытые вопросы

- (нет)
