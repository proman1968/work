# Прогресс: $ai / task.ai

## Последние изменения

- [01:12] **Детерминированный переход при единственном маршруте.** `active_pipe.next.length === 1` → переход без меню и без вызова модели (`prompt → thinking`, `plan → task`, `task → step`). Снят дрейф GigaChat: после «Принять» плана модель выбирала `complete` вместо `task` (снимок `1785794413435`) — теперь выбора нет. `complete` предлагается только на реальных развилках (`next.length > 1`).
- [01:12] **Режим `autocomplete` для служебных контейнеров.** Флаг узла в `pipe.js` (`research`, `search`, `ask`, `action`): выбранный из меню `complete` пушится без `button`, контейнер закрывается сразу (`closed = true` + `_advance_steps` + save + auto-loop) — пользователь ничего не подтверждает. `step` и корневой `prompt` — по-прежнему с кнопкой «Завершить».
- [00:59] **`prompt` узлов `research`/`search`/`ask` — строки, не массивы.** Забытый `.join()` отправлял в GigaChat `content: [...]` → `400 invalid JSON syntax` (снимки `1785792604829`, `1785794191133`).
- [00:55] **Позиция автомата при закрытом ребёнке — контейнер.** `_active_block`: если последний ребёнок закрыт — вернуть сам контейнер, не закрытого ребёнка. После «Завершить» шага маршрут = `pipe.task.next = ['step']` → строгий прогон всех шагов task (был баг: маршрут от закрытого step уводил в thinking-сиблинг, step2 не инициировался).
- [00:55] **Крестик не завершает.** Ветка подтверждения `complete` срабатывает только когда `prompt` служебного хода совпадает с `leaf.button.label` («Завершить»); отказ «нет» — мимо.
- [00:55] **`complete` не предлагается на уровне task.** Task закрывается только автоматом после последнего шага (`_advance_steps`); в меню контейнера task — только `step`.
- [00:50] **step — промпт-блок.** `step.build(r, ctx)`: заголовок = «N. описание» из `ctx.currentStep` (движок передаёт in_progress-шаг активного task), ответ модели → первый thinking внутри `items`. В `context()` step уходит как `role:'user'` (инструкция). `_advance_steps`: все шаги done → `task.closed = true`. UI: `microchat-view-step` (extends prompt-view).
- [00:07] **`research.build`: `type: 'research'` (был `'thinking'`).** Из-за неверного типа после выбора research маршрут брался из `pipe.thinking.next` → меню thinking повторялось, и research-блоки маркировались как thinking (снимок `1785791116738`: два лишних «thinking» с icon search). Теперь leaf после research имеет тип `research` → маршрут из `research.next = ['search','ask']` — корректный спуск.
- [00:07] **Цикл пропуска роутеров в `prompt()`.** Узлы без `build` (search, ask) — роутеры: не исполняются и не пушатся, меню перестраивается от их `next`, модель спрашивается снова, пока не выбран узел с `build`. Снимает краш на `next_pipe.build(response)`/`next_pipe.prompt` для роутеров.
- [00:03] **Auto-add `complete` в меню.** Если у активного контейнера есть `items` — в меню выбора добавляется пункт `complete` (не пишется в `next` узлов). Снимает «нельзя закрыть шаг/контейнер».
- [00:03] **Обработка подтверждения `complete`.** Служебный ход (`role:'AI'`), когда лист = `complete`-блок → `_active_container().closed = true`, `_advance_steps` продвигает `task.steps` (in_progress → done, следующий pending → in_progress), save, auto-loop от родителя. Снимает «после complete ничего не происходит».
- [00:03] **Проброс `user` в auto-loop.** `this.prompt({ role:'AI', user })` — сессия передаётся по цепочке вызовов через замыкание, без хранения. Снимает «на клиенте только thinking» — `_save`/`chat.done` теперь доходят до клиента и в служебных вызовах.
- [23:45] **Разделение `_active_block` (лист) и `_active_container` (родитель).** Маршрут (`_active_pipe`) берётся от типа листа — восстановлено поведение до коммита 1: после `thinking` меню = `plan/research/action/report`, а не зацикленный `thinking`. Пуш (`_push_block`) идёт в контейнер — после close кладётся сиблинг, без краша на `leaf.items.push`. Откат регрессии коммита 1.
- [23:45] **Валидация выбора модели в `prompt()`.** Ответ модели нормализуется (чистка пунктуации/кавычек, сплит по словам), берётся первое слово, совпадающее с пунктом меню; если нет — fallback на первый пункт. Снят краш `Cannot read properties of undefined (reading 'prompt')` из снимков `1785779432227` / `1785778538923`.
- [23:45] **Терминал обрабатывается без краша.** Если у активного узла нет `next` (report/complete/action/tool) — маршрут не строится, поток завершается тихо. Подтверждение кнопок и close контейнера — отдельно.
- [20:30] **`step` — контейнер, не инъекция.** Появился `build` (блок `step` с `items: []`, `next: ['thinking']`) и `inject`. Внутри шага идёт тот же цикл `thinking → …`, `complete` закрывает шаг и поднимает к `task`. Снят блокер «step без build ломает контракт» и бесконечный выбор `task.next = ['step']`.
- [20:30] **`_active_block` — подъём к родителю после close.** Спуск только в открытый контейнер с `items`-массивом; если последний ребёнок закрыт или лист — активен родитель. Зеркало логики `get_active_list`. Снят блокер «_active_block после close возвращает закрытый блок».
- [18:15] **pipe.js: flatten + узел `complete`.** Убраны `root` и обёртка `nodes` — узлы лежат линейно на верхнем уровне (`pipe[id]`, не `pipe.nodes[id]`). Добавлен `complete`: обычный узел (prompt + build с `button: 'Завершить'`), после подтверждения закрывает текущий контейнер. Движок добавляет `complete` в меню выбора автоматически для блоков с `items`.
- [18:15] **`button` — единственный источник правды в блоке.** Убраны `button` с уровня узла в `form`/`questions`/`report` (дублирование с `build`). Движок судит об остановке по `block.button`, не по `next_pipe.button`. Поле `next_pipe.stop` оставлено как задел для остановки без кнопки (вопрос/ответ пользователю).
- [12:00] **Сверка readme vs код.** Признано: readme описывает целевой дизайн, код ему не соответствует — большая часть заявленного отсутствует (`_walk`, FC, harness tools, `complete_step`, `_resolve_button`). Канон правки — только код трёх файлов.

## В работе

- `answers` из `confirm()` — деструктурировать в `prompt()` и прокидывать в messages.
- FC-слой: `functions` в `_streamChat`, диспетчер tool-вызовов по `name`, `tool_result` блок.

## Ключевые решения

- **Состояние автомата = тип последнего блока в дереве**, не отдельное persisted-поле. Маршрут берётся из `pipe[_active_block().type].next`. Persisted `pipe_node` — заглушка, удалится.
- **Поток направленный: вперёд или вверх.** Любой узел с `next` углубляет дерево или пишет sibling'ом; только `complete` закрывает контейнер и поднимает на уровень родителя. `body.closed = true` — терминал, задача закрыта навсегда.
- **`complete` добавляется в меню автоматически** для блоков с `items`; в `next` узлов его не пишем. Меню строится в `prompt()` по `active_pipe.next` + опциональный пункт `complete - <inject>`.
- **`button` только в `build`.** Узел не несёт `button`; блок — единственный источник правды для UI и для движка (`if (!block.button && !next_pipe.stop)` → продолжать auto-loop).
- **Служебная роль переименована: `AI`** (в коде `role === 'AI'`), не `ASSISTENT`. Служебные ходы auto-loop и подтверждений кнопок идут с `role:'AI'`.

## Блокеры / Открытые вопросы

- Продвижение шагов `task.steps` после close шага: кто и когда ставит `in_progress → done` и выбирает следующий `pending`. Пока без этого `task.next = ['step']` создаст сиблинг-шаг, а не продолжит план.
- FC-слой не подключён: `_streamChat` не передаёт `functions`, `r.calls` всегда пустой, `build` FC-узлов всегда уходит в text-ветку.
- Исполнение tool-вызовов: `build` узлов `web`/`file`/`action` возвращает `{type:'tool', name, args}`, но блок с типом `tool` не имеет позиции в `pipe` → следующий шаг падает. Требует решения: либо тип узла как тип блока (`type:'action'` для action-узла), либо отдельный `tool`-узел в pipe.
- `answers` из `confirm()` не доходят до модели: `prompt()` не деструктурирует `params.answers`.
- Меню выбора: `complete` для контейнеров ещё не добавляется автоматически; обработка подтверждения `complete` (закрытие) не реализована.
