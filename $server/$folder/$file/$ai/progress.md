# Прогресс: $ai / task.ai

## Последние изменения

- [19:05] Ситуативный servicePrompt + толерантность к кириллическим каналам (разбор прогона 18:45 / `1785080727330` — отказ «Шаг 1 уже закрыт» сработал, но модель ответила `<план>{steps}</план>` + `<tool>propose_plan</tool>`, парсер не распознал, сырой XML утёк в text, ход умер; заодно шальной action «Сбор данных…» с кнопкой посреди Do): 1) `driverDirective(body, entry, role)` — харнесс сам вычисляет ситуацию (step-prompt clarify/do по `stepNeedsClarify`+`stepEvidence`, Plan-фаза, финальный Отчёт, tool_result шага) и настраивает reasoning на выбор действия первой строкой («Действие: …»/«Тип: …») с конкретными номерами шагов в complete_step; статичные меню `TYPES.prompt`/`thinking` сокращены до fallback; 2) `normalizeChannelTags` — `<план>/<подплан>/<вопросы>/<форма>/<действие>/<мысли>` → латиница; `<tool>имя</tool>` вычищается как FC-мусор; 3) план при активной задаче → `pendingSubplan` текущего шага (декомпозиция, не второй «План»+«Начать»); 4) `demoteStrayDoActions` — action-кнопки модели посреди Do (кроме «Отчёт»/«План») понижаются до text (action в `WAIT_USER_TYPES` — мёртвая кнопка вешала бы поток). Тесты: 235 pass (+12).
- [18:41] Фикс teach→remap после clarify (разбор прогона 18:34 / `1785080071018` — шаг 1 авто-закрыт, модель повторила ask_user → teach велел complete_step → `complete_step({step:1})` молча ремапился на шаг 2 и требовал save_file → проза): 1) `completeTaskStep` на уже `done` номер — явный отказ «Шаг N уже закрыт. Текущий — M…», без fallback на in_progress; 2) `duplicateAskTeach` — факты ответов + текущий открытый шаг / tool/save_file, без инструкции закрыть закрытое. Тесты: plan-steps +3.
- [18:35] Tap-first ворота (разбор прогона 18:21 — после удаления фабрикаторов GigaChat Light прислал вопросы без options, пользователь получил три пустых textarea; мобильному нужны тапы, не ввод): 1) опрос с полями без вариантов (type text — неявное понижение из select) не показывается — модели уходит обучающий tool_result «дай options: 3–5 вариантов из контекста + „Другое“; свободный текст — только type textarea»; 2) стоп-кран: один ретрай на span (до следующего входа пользователя, `hasRecentAskOptionsTeach`) — второй раз открытые поля принимаются, лучше textarea, чем мёртвый цикл; 3) канон и USER-оверлей: «пользователь на мобильном — тапы вместо ввода», обновлены описания FC-схемы ask_user и clarify-подсказка step-prompt. Источник вариантов — только модель и контекст задачи, харнесс по-прежнему ничего не выдумывает. Тесты: 220 pass.
- [17:55] Убраны доменные фабрикаторы (последние «костыли под презентацию»): удалены `makeClarifyQuestions` (пресеты «Тема презентации?/Сколько слайдов?» и общие «Что важнее уточнить?») и `defaultOptionsForAskField` («Вариант A/B/Другое», темы, 5/8/12/15). Теперь: `ask_user` без вопросов → обучающий tool_result «сформулируй questions по описанию шага», не сфабрикованный опрос; поле без ≥2 options от модели → открытое text/textarea (варианты даёт только модель); вопрос без формулировки и без id отбрасывается. Универсальный поток не привязан к домену. Тесты: 218 pass.
- [17:45] Анти-цикл clarify (разбор прогона 17:35 — модель трижды переспрашивала тему/цель, шаг 1 не закрывался): 1) `autoAdvanceClarifyStep` — ответы на опрос clarify-шага закрывают шаг детерминированно на входе promptTurn (харнесс сам вызывает `completeTaskStep` с summary из label: value и пушит «Выполни шаг N+1»; слабой модели не доверяем вызвать complete_step); do-шаги не трогаются — им нужен tool; 2) `findAnsweredDuplicate` — повтор уже отвеченного опроса (по нормализованным label, независимо от id/порядка) не попадает в ленту: вместо него обучающий tool_result с готовыми ответами и инструкцией complete_step. Тесты: 216 pass.
- [17:35] Ворота качества плана (разбор прогона 17:12 — GigaChat Light выдал «План: 1. Шаг» с кнопкой «Начать»): 1) объектная ветка `<plan>{…}</plan>` разворачивает обёртку `{steps: […]}` и больше не фабрикует шаг «Шаг» из объекта без description — вместо этого `planRejected`; 2) `planQualityError`: <2 осмысленных шагов (пустые/«шаг N») → обучающий tool_result «вызови propose_plan({steps})» вместо кнопки «Начать»; отказ снимает и action «План», созданный парсером. Тесты: 210 pass.
- [17:30] Надёжный протокол каналов (разбор прогона 16:47 — план и вопросы уехали в незакрытый `<reasoning>`, textarea превратился в select «Вариант A/B/Другое»): 1) структурные каналы — native FC-tools `propose_plan({steps, intro})` и `subplan({steps})` (перехват в promptTurn до executeToolCall, путь `pendingPlan`/`pendingSubplan`; шаги принимаются и объектами, и строками); XML-теги остались fallback'ом; 2) reasoning не глотает каналы — thinking обрезается на первом теге `<plan|subplan|questions|form|action|tool_call`, хвост уходит в обычный разбор (закрытый и незакрытый случаи); 3) толерантный `<plan>`: невалидный JSON → шаги из нумерованного списка (`parseNumberedListSteps`); 4) открытые поля — явный `type: textarea|text` без options не апгрейдится в select с выдуманными вариантами (фабрикация дефолтов только для select/без type); 5) впрыск состояния в Do-блок system: evidence текущего шага (опрос/tool — примется ли complete_step), реальные артефакты `collectArtifacts` (или «не упоминай несуществующие файлы»), остаток авто-ходов; 6) канон в servicePrompt: «закрой `</reasoning>` до канала», «`<plan>` — строго JSON-массив». Тесты: 206 pass.
- [16:35] Анти-галлюцинации и возврат пользователя в цикл (разбор прогона 13:00 — все 5 шагов закрыты прозой, файлов нет, артефакт в Отчёте выдуман): 1) ворота на `complete_step` — clarify-шаг требует answered questions/form в span'е шага (отказ учит вызвать ask_user), do-шаг — успешный tool_result (отказ учит save_file); 2) `stepNeedsClarify` переписан (глагол+объект, словоформы без ложного «сис|темы»), мёртвая ветка `getDoStepPhase` починена — clarify-фаза видна и сразу после step-prompt; 3) step-prompt clarify-шага несёт подсказку «начни с ask_user, не выдумывай значения»; 4) честный Отчёт — `collectArtifacts` подставляет реальные file-пути в финальный prompt (или «артефактов не создано — не выдумывай»); 5) позиционный `save_file("имя")` прозой → обучающий tool_result вместо тишины; 6) USER-оверлей: текстовый формат артефакта (html/md), не pptx/docx. Тесты: 191 pass.
- [15:55] Полный цикл Do: движок шагов — harness-tool `complete_step({step, summary})` ставит `done` и пушит «Выполни шаг N+1» (раньше шаги никогда не продвигались — «жди систему» из tool_result было пустым обещанием); последний шаг → prompt «сформируй Отчёт» → «Принять» закрывает задачу без хода модели. `<subplan>` теперь применяется (раньше парсился и выбрасывался): вложенная подзадача-стек, закрытие подшагов закрывает шаг родителя. Парсер принимает атрибутную форму `<action title=… label=…>` (причина последнего тупика GigaChat Light) и вычищает сырые теги каналов из text. Ролевые оверлеи `ROLE_OVERLAYS` к servicePrompt драйвера: USER — артефакт-first, BOSS — делегирование/spawn_agent, ADMIN — inspect→edit(diff)→verify+readme. Интернет: `fetch_url` (HTML→текст, 20k) в сервисе SearXNG + ветка «внешние факты → search → fetch_url» в классификации хода. Тесты: plan-steps 17 кейсов, всего 181 pass.
- [15:20] Фикс авто-хода: `Reactor#async` (reactor.js:137) падал на сервере с `ReferenceError: requestAnimationFrame is not defined` (голый идентификатор, Node без rAF) — теперь `globalThis.requestAnimationFrame ?? queueMicrotask`. Симптом: лента обрывалась на thinking-блоке после ответов на questions, UI висел со спиннером. Регрессионный тест в smoke-core (169 pass).
- [15:10] Устойчивость к сетевым сбоям LLM: `classifyStreamError` (transient/fatal), авто-retry стрима с backoff (до 3 попыток), при исчерпании — action «Повторить» + `body.pendingRetry{turn}` вместо мёртвого error; fatal (401/403/422) — error с пояснением. Причина инцидента — DNS-фильтр ZenSafe рвёт TLS к api.z.ai (не баг кода).
- [11:55] Весь харнесс — в `class.js` типизатора: `prompt` — инстанс-метод файла (merge-наследование), один проход, авто-ходы через `this.async`; удалены `methods/prompt/$method` и `sources/modules/ai-prompt` (legacy.js, usage.js); тесты переведены на импорт из `$ai/class.js` (161 pass).
- [21:05] `prompt.execute` — тонкий TYPE-driven пайплайн (`servicePrompt` → LLM → TYPE → auto/stop); хелперы в `sources/modules/ai-prompt/legacy.js` (§1.11).
- [12:38] Убран `sources/modules/task-control` / `body.control`; протокол хода — `TYPES.servicePrompt`; harness — в `methods/prompt`.

## В работе

- Фаза 5: trust markings UI + plan→diff→apply для `$class`/handlers.
- Параллельные subagents + merge.

## Ключевые решения

- **prompt = метод файла.** Наследуется из `class.js` типизатора; один проход, продолжение — `this.async(() => prompt(...))`, без блокирующего while-цикла.
- **system = identity; ход = servicePrompt.** Протокол после блоков ленты.
- **body.usage = истина по задаче.** Все LLM-ходы, включая system/servicePrompt.
- **Фаза = данные файла.** `pendingPlan`, `task` в ribbon, статусы шагов — без параллельного `body.control`.
- **Plan-first / Cursor-analog.** Фазы 1–4; TTS = Piper; JSDoc канон.

## Блокеры / Открытые вопросы

- (нет)
