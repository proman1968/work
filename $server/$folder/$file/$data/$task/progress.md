# Прогресс: $task

## Последние изменения
- [15:05] Инструмент `report` в `task.js`: удалены `stop` и `approve`. Причина: отчет — закрывающий документ без одобрения, одобряются только действия.

## В работе
- Нет.

## Ключевые решения
- Решение: вычисляемое — только чистые геттеры, инвалидация — записью `undefined` в вычисляемое свойство. Причина: каскад зависимостей и уведомление выполняются реактором, ручной кэш дает цикл уведомлений.
- Решение: `body.mode` `plan`|`build`; старое `do` в JSON — алиас `build`. Причина: открытые сессии не должны тихо упасть в меню plan.
- Решение: `need=side` без explore/web/logs — только сбор, не work. Причина: thinking → сразу work, изучения не было.
- Решение: `stop` — признак остановки фокуса, не временный флаг. Причина: снятие `stop` прятало кнопку на activation.

## Блокеры / Открытые вопросы
- Нет.
