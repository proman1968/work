# Прогресс: $task

## Последние изменения
- [10:30] `on_save`: `buildSystemPrompt` из `ai/prompt/$method` (harness удалён). Причина: one-shot и system — один модуль.

## В работе
- Канон оркестратора: локальный `tools` vs `$class/ai` `moves`.

## Ключевые решения
- Решение: session на `$task`, one-shot в `$class/ai/prompt/$method`. Причина: разные контракты.

## Блокеры / Открытые вопросы
- Нет.
