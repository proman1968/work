# Прогресс: $task

## Последние изменения
- [23:31] Док: html-отчёт сохраняется как `.html` (`block.html`), не `.md`.
- [23:10] set `$item` не ставит `pending` из `streamTarget` — повторный вход в остановленную задачу даёт «Продолжить».
- [18:23] `data.menu` в `title` у `details` при `stop: true`.

## В работе
- Харнесс линейный; подпись там, где появился content

## Ключевые решения
- `box.content` режет выбор шага. Label — короткий запрос к модели, не лента и не нарезка.

## Блокеры / Открытые вопросы
- —
