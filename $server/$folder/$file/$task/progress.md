# Прогресс: $task

## Последние изменения
- [18:55] В каждый `context()` — живые дата и время. Точка — из браузера в `prompt` (`here` → `session`), не на `$user` и не на диск.

## В работе
- Стоп на `text` после промаха меню
- `execute` в корневом меню не включать

## Ключевые решения
- На пустоту не биндить.
- Место в запросе важнее GPS.

## Блокеры / Открытые вопросы
- —
