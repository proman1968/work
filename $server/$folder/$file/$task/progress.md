# Прогресс: $task

## Последние изменения
- [15:20] Узлы `pipe.js` — `export const`, без объекта `PIPE`. Харнесс грузит namespace модуля (`this.pipe[type]`).
- [15:20] Удалены мёртвые `close_up` / `childRollup` и хвост хелперов старого автомата.

## В работе
- Разбор харнесса

## Ключевые решения
- Таблица типов = модуль, не второй реестр с тем же смыслом.
- Первый запрос — `body.title`.

## Блокеры / Открытые вопросы
- —
