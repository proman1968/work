# Прогресс: $task

## Последние изменения
- [18:57] Полоска тела ленты только у контейнера (`data.items` — массив), не у листьев.
- [18:50] Убрал потолок `WEB_SITES`: очередь сайтов = все уникальные url из search (лимит сервиса).
- [18:26] Откатил цвет/кегль тела ленты. Viewer снова только `:light="showTitle && !pinned"`.

## В работе
- Тот же паттерн атомов у `work`
- В `plan` нет пути к `complete` после обзора

## Ключевые решения
- Правки темы тела отменены: ломали markdown.

## Блокеры / Открытые вопросы
- —
