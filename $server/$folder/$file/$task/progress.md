# Прогресс: $task

## Последние изменения
- [10:05] Текст вложений и атома `read` — через `$file.read_text()` (сервер), не Kreuzberg из `class.js`.
- [09:50] Вложения пишутся в `items` как контейнер `includes` + дети `file` — в ленте, не в корневом `includes`.
- [09:50] Харнесс читает `file` и закрывает `includes` через `done.prompt`.

## В работе
- Включить `work` в `execute.next`, когда понадобится запись из площадки исполнения

## Ключевые решения
- Корневой `body.includes` не использовать: лента — только `items`.
- `on_save` при непустых `items` входит как `AI`, чтобы не дублировать `prompt`.
- Чтение текста файла — `$file.read_text()`, не импорт Kreuzberg из `data:`-модуля `$task`.

## Блокеры / Открытые вопросы
- —
