# Прогресс: $task

## Последние изменения
- [09:54] Док рисует тот же `microchat-view-*`, что лента; `onlyDoc` прячет шапку и детей.

## В работе
- Тело отчёта в доке — тип блока, не отдельный markdown

## Ключевые решения
- Один `viewTag` на ленту и док. `onlyDoc` только на view, в JSON блока не пишется.

## Блокеры / Открытые вопросы
- —
