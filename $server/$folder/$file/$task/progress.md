# Прогресс: $task

## Последние изменения
- [17:42] `limit` считается после последнего user-`prompt`. Новый запрос снова даёт обзор. У `explore` — `limit: 1`.

## В работе
- Стоп на `text` после промаха меню
- `execute` в корневом меню не включать

## Ключевые решения
- Окно обхода — от последнего `prompt`, ленту не чистим.

## Блокеры / Открытые вопросы
- —
