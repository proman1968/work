# Прогресс: $task

## Последние изменения
- [02:09] Отработанный `report` снимается из `items` (`drop` после `close_up`). Текст отчёта остаётся в `container.content`; `continue` в ленте не висит.

## В работе
- `includes.next` только `report` — после `continue` меню пустое, смотреть на живом таске
- `execute` в корневом меню не включать

## Ключевые решения
- Лист `report` одноразовый: закрыл или сказал continue — блока больше нет.

## Блокеры / Открытые вопросы
- —
