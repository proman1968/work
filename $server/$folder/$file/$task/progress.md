# Прогресс: $task

## Последние изменения
- [17:45] В `form.prompt` выбор только `select` + «Другое». Radio/checkbox убраны — в одном таске модель рисовала разный зоопарк радиокнопок.

## В работе
- Включить `work` в `explore.next` / `execute.next`, когда понадобится файловый контур

## Ключевые решения
- Один вид выбора — `select`. `legend` — имя поля, не путь.

## Блокеры / Открытые вопросы
- —
