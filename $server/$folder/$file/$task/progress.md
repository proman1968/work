# Прогресс: $task

## Последние изменения
- [11:42] Место — `body.location`, один раз в `on_save`. `prompt()` больше не пишет gps.

## В работе
- Стоп на `text` после промаха меню
- `execute` в корневом меню не включать
- Липкость шага (офсет / стопка summary)

## Ключевые решения
- `content` контейнера только после `report`, не из собственного стрима
- Вход `execute` — в `system`, закрытие — `STAGE_PROMPT`
- Исполнитель без результата закрывается ошибкой, не стримом модели

## Блокеры / Открытые вопросы
- —
