# Прогресс: $task

## Последние изменения
- [17:49] Sticky ленты: одна поверхность на блок — `todo`/`prompt` липнут хостом, контейнер только `summary`.
- [17:49] Убран двойной `position: sticky` (хост + summary на одном скролле) — шапки больше не наезжают друг на друга.

## В работе
- Разбор харнесса

## Ключевые решения
- `host-sticky` — attr + CSS `top: var(--chat-sticky-top)`. `headerStyle` задаёт полку, не пишет `position` на оба слоя.

## Блокеры / Открытые вопросы
- —
