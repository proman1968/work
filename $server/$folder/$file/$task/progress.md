# Прогресс: $task

## Последние изменения
- [09:44] Признак дока стал врождённым: `doc: true` на узлах `file` / `web` / `execute` / `check` / `planning` / `report` / `html`, копируется в блок в `_build_block`. Динамические `block.report = true` в `activation` / `planning` / `html` / `report` удалены; предикат дока в UI — `b.doc && b.content` (боксы больше не доки автоматом).
- [09:44] Гвард повторного разбора в `html.recalc` — по `block.html`, а не по флагу отчёта.
- [09:44] Маркер закрытия `includes` — имена файлов через запятую, не количество: состав вложений виден в ленте и любом потребителе `box.content`.

## В работе
- Прогон сценариев: док собирает отчёты `file` и итоги `web`/`execute`/`check`; черновик `report` виден в доке до «Принять».

## Ключевые решения
- Решение: `doc` — свойство узла pipe, не динамический флаг. Причина: тип блока сам знает, документ он или речь; четыре разрозненных сеттера заменены одним объявлением.
- Решение: старые ленты с `report: true` в доке не поддерживаем (без шима в предикате). Причина: тестовые данные, правило против обходов.

## Блокеры / Открытые вопросы
- Черновик `report` теперь в доке во время стрима (до approve) — проверить, не мешает ли.
