# Прогресс: $task

## Последние изменения
- [10:36] Вход USER в `prompt()` сохраняет `post.files` и пушит новый `includes` — микрочат слал FormData, метод читал только текст.

## В работе
- Стоп на `text` после промаха меню
- `execute` в корневом меню не включать

## Ключевые решения
- Новый `includes` на ход, не дописывать закрытый «N/N»

## Блокеры / Открытые вопросы
- —
