# Прогресс: $task

## Последние изменения
- [12:42] Data-узлы (`includes` / `file` / `web` / `site` / `search` / `read`) получили `role: 'user'` — данные в контексте отделены от речи агента: слабые модели игнорировали вложения внутри склеенного assistant-монолога.
- [12:42] `includes` закрывается без LLM: `recalc` ставит маркер `[attachments] файлы: N`, `expand: true` отдаёт контексту отчёты `file` напрямую — нет второй производной, потерь и захвата файлов из соседних `includes`; `includes.prompt` удалён.
- [12:42] `context()` / `_box_context(box, focus, evidence)`: предки дают рамку (`prompt`-блоки + закрытые боксы + `answer`), фокус — все детали; генерация `total` идёт с `evidence: false` — без чужих сводок.
- [12:42] `file.prompt`: числа, таблицы, идентификаторы — дословно (отчёт по файлу теперь единственный носитель содержимого).

## В работе
- Проверка сценария вложений (задача «Привет») на локальной модели: закрытие `includes` маркером, вложения в финальном ответе.

## Ключевые решения
- Решение: прозрачность бокса (`expand`) вместо свёртки отчётов в `box.content`. Причина: свёртка дублировала бы отчёты в JSON и history-снимках; один источник правды — сами `file`-блоки.
- Решение: инвариант ролей — данные `user`, речь `assistant`; роль объявляется в узле pipe. Причина: двухролевая схема без `tool`-роли; данные в assistant-монологе модель считает «уже сказанным».

## Блокеры / Открытые вопросы
- Роли пограничных боксов (`explore` / `work` / `execute` / `check`) — вне пакета, решить после прогона.
