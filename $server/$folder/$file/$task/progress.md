# Прогресс: $task

## Последние изменения
- [00:50] Sticky: `stickyGen` на ленте + замер шапки в `attached` — при живом `load()` все `top` пересчитываются, дыры между шапками не остаются.

## В работе
- Разбор харнесса

## Ключевые решения
- Кто липнет — без изменений. Только инвалидация offset после высоты шапки.

## Блокеры / Открытые вопросы
- —
