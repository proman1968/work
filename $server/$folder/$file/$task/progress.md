# Прогресс: $task

## Последние изменения
- [14:57] Капча и пустая страница — `error`, не успех. В меню `site.limit` считает только `siteOk`; `report` без успешного сайта не открывается. Причина: Яндекс-капча закрывала `web` и остальные url из выдачи не открывались.

## В работе
- Стоп на `text` после промаха меню
- `execute` в корневом меню не включать

## Ключевые решения
- Ошибка захода не занимает квоту и не даёт закрыть этап.

## Блокеры / Открытые вопросы
- —
