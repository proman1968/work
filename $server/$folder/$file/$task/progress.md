# Прогресс: $task

## Последние изменения
- [01:28] `web.label` больше не заменяется запросом — шапка «Интернет». Без текста `prompt` в корне только `question`. Поиск не берёт мысли как запрос.

## В работе
- Стоп на `text` после промаха меню
- `execute` в корневом меню не включать

## Ключевые решения
- Нет задачи — спросить, не искать и не писать мысли в ярлык.

## Блокеры / Открытые вопросы
- —
