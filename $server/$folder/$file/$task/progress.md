# Прогресс: $task

## Последние изменения
- [15:59] `$task` = хранитель сессии: harness/agents/task.js перенесены в `$structure/ai/`; `class.js` делегирует; `_methods` видит `~/ai/*` ($method). Причина: ИИ — способность класса, файл — журнал длинных задач.

- [15:06] `planning` / `report` → `agents/` (`step: false`); оркестратор `moves` = thinking|answer; `parsePlanMarkdown` — named export. Причина: план/отчёт — навыки, не ходы оркестратора.

- [14:59] Канон агента: `export default { moves, tools }`. Оркестратор — ходы в `moves`, `tools: {}`; loader/`topicsMap` — ходы|инструменты|агенты. Причина: ходы жили в `tools`, ломая один смысл слота у всех агентов.

- [14:33] `_box_context`: `box.system` + `pipe.system` склеиваются (не `||`). Причина: on_save писал Рязань в `body.system`, оркестраторский system его перекрывал → ложный question про город.

- [14:16] `web`: убран tool `query` — поиск снова в `web.init`, меню только `site`(+total). Причина: total проталкивал текст запросов без site; лишний выбор модели.

- [14:04] Scoped `prompt({ agent })`: не выходить по `box.error` (частичный fail site) — только по `content`. Причина: `@web` останавливался после первого fetch failed.

- [13:58] `prompt({ agent })` / `@web …`: scoped-прогон субагента до `content`, возврат `{ ok, agent, content, waiting? }`; panel передаёт `agent`. Причина: навык как вызываемый результат (UI и мастер-агент).

- [13:31] `form` / `question` → `agents/` (`step: false`); оркестратор tools: thinking|answer|planning|report. Причина: диалог — наследуемые агенты, не ходы.

- [13:24] Оркестратор = `export default` + `tools` (ходы); субагенты только discover `agents/*` (без `dispatch`/`AGENTS`); меню корня = ходы ∪ агенты; `step` = thinking ∪ агенты. Причина: один паспорт агента и tilde без allowlist.

- [12:49] Агенты как файлы: `task.js` + `agents/{web,work,html}.js` (`export default` + `tools:{}`); loader в `class.js` (next из keys tools + total); `dispatch` / `TASK_STEPS` / `answer` в корне; brief при push; поиск — `tools.query`. Удалён `pipe.js`. Причина: ход≠агент≠tool и tilde-переопределение навыков.

- [10:26] Inject без жаргона узлов: `answer`/`report`/`question` — «факты в контексте», «сводка в ленте», не «закрытый web».

- [18:32] `answer` vs `report`: после закрытого web — короткий answer; report не пересказывает один web. Причина: двойная писанина (web.content + отчёт с теми же ценами).

- [18:21] Mode-brief: у LLM-листов `system` (роль хода); `context({ leaf })` дописывает его при стриме. Сначала thinking/answer/question (+ planning/form/html/report/write). Причина: open-code–style контракт режима без раздувания body.system.

- [18:12] `pipe.js`: убраны legacy-узлы `explore`/`execute`/`check`/`thought`/`repeat`, закомментированные init и орфан-хелперы site. Причина: разработка, старых лент нет.

- [18:03] `web.inject` — «поискать информацию в интернете» (вместо канцелярита про факты/контекст).

- [18:01] Карта в context: один блок `[доступные инструменты]` = next фокуса минус using; пара `[задача]`/`[далее]` убрана (на корне почти дублировалась).

- [17:50] `answer` vs `report`: answer — реплика пользователю; report — сводка длинной работы/плана, не вместо ответа. Без частных жанров в inject.

- [17:43] Silent `total` (`init` false): auto-loop по закрытому box, не по ghost-листу. Причина: web закрылся с погодой, `_canLoop(total)` → chat.done, на task не было `answer`.

- [17:35] `web`: пока 0 успешных site и очередь URL жива — `using_blocks = ['total']` (форс site); ранний total больше не гасит этап. Причина: погода — fetch failed → total → chat.done, остальные URL не тронуты.

- [17:20] Все `inject` меню — короткий позитив «когда брать»; `question` без «доступа/дыры», `web` — «факты из сети». Причина: ложный question вместо web (погода / «нет интернета» в thinking).

- [16:29] `context()`: подряд `assistant` не склеиваются — между ними user `продолжай`. Причина: thinking+html+report в одном сообщении, модель не видит границы ходов.

- [15:56] `step` = только исполнение (`STEP_NEXT`: thinking|web|work|html). Диалог — наверху: `todo.next` = question|form|step|report. Убран nudge «нужен question» из instruction шага. Причина: матрёшка plan и вопрос про default внутри step (сапёр / gemma).

- [15:40] `unwrapFence` снимает первую пару \`\`\` и **оставляет хвост** (подпись form). `keepFence` убран — `_applyStream` unwrap для всех листов. Причина: спецфлаг был костылём под старый unwrap «только если \`\`\` в конце».

- [15:37] После стрима `_applyStream` снимает внешний \`\`\` (`unwrapFence`) у всех листов. Причина: planning/answer с \`\`\`markdown рисовались как code-block.

- [15:23] Режим plan/do убран из глобального `body.system` и из `modeLine` в `context()`. Текст — только `work.plan.system` / `work.do.system`. Причина: модель отвечала «я в plan» вместо действия (сапёр).

- [13:41] `details` без flex — закрытие снова прячет тело (файл не наезжает на отчёт). `includes` с детьми не рисует маркер `[attachments]` вторым блоком. Причина: дырка под файлом и схлопнутый отчёт.

- [13:24] Док снова один слот: `~is="viewTag"` + `:data="current"` + `only-doc`. Убран `~for` с новым массивом — тело не монтировалось.

- [13:00] `report.stop = true` — нет «Принять». Док: без шапки оболочка `div`, не `details` (тело больше не прячется до F5). Причина: кнопка крутила отчёты; onlyDoc + details = пустая панель.

- [12:35] `mode`: do только `activation` в `work`; `plan` при закрытии `work` (`total`). `report.approve` mode больше не ставит. Причина: отчёт не должен крутить режим.

- [12:21] Слот «Рассуждаю» только если бар не `off` и у стрима `allowReasoning`. Иначе reasoning-чанки отбрасываем (модель всё равно может слать CoT). Флага на узлах нет — слота нет. Причина: экран рисовал CoT независимо от флага.

- [11:54] Потолок ответа — `maxOutput` на узле в `pipe.js`. Нет поля — `$task` не передаёт, `$ai` не ставит `max_tokens` и не режет 4k/8k. Silent (меню/шапка) — 64. Причина: html обрезался скрытым дефолтом карточки.

- [11:42] `_push_block`: нет `box` — корень `body`; `using_blocks` через `push`/`includes`, не ODA `.add` (в Node его нет — падение процесса). `_build_block` терпит неизвестный тип. `catch` больше не пушит error без box.

- [11:32] Нет непустого `content` у листа — пайп не идёт дальше: «Продолжить» достримляет тот же блок; пустой стоп не пишет `content` и снимает тип с `using_blocks`. Причина: обрыв thinking + Продолжить открывал web.

- [11:11] Таблицы в ленте: промпт `file`/`site`/system — md ≤5 колонок, длинное списком, не простыня «дословно»; `.body` — `overflow-x: auto`. Причина: обзор xlsx (75×длинные ячейки) нельзя было смотреть.

- [10:55] Маршруты Деминга: `task`/`step` один `next` (обзор web|work → planning/activation/execute → check/report). `explore` не в меню. `mode=do` только `activation.approve` (write в work); снято с planning/todo/check. `prompt.next` — thinking/answer. Причина: explore — лишний хоп; план и проверка не должны открывать запись файлов.

- [17:58] Тело блока — только `content`; вид по `type` (`html` → iframe, `form` → слот из fence). `html.recalc` снимает fence в `content`, поле `html` не пишется. `form` больше не режет стрим на `html`+подпись. UI/док/streamTarget/шапка читают `content` (старый `block.html` — фолбэк). Причина: два поля давали markdown+iframe и «плавающую» шапку SPA.

- [17:38] Меню: критерий выбора вместо «подходящий и логичный» — быстрее закрывает запрос; stop (вопрос/форма) только когда без человека нельзя; default/план уже в контексте → действовать. `question.inject` — дыра = нет данных/доступа у системы, не параметры с default; `activation.inject` — решено в размышлениях → activation. Причина: Qwen 27b при готовом плане Сапёра в thinking выбирал `question` про размер поля и стопался.

- [17:19] Роли меню (не спец-кейс): `activation` = артефакт/код → do; `explore` = только внешние факты, не «написать известное»; `question` = не вкус пресета. В `task`/`step`/`execute.next` activation/execute/html раньше explore.

- [17:09] `web` после LLM-`total`: в `content` дописываются `![…](url)` из успешных `site` (раздел «Медиа») — картинки не зависят от пересказа модели.

- [17:02] `site`: после захода — пока URL есть и успешных &lt; 3, site снова; иначе `using_blocks = ['site']` → принудительный `total` (не вычитывать 6–8 страниц).

- [16:52] Меню/шаг: `explore`/`question`/`planning` inject — не повторять тот же поиск, дыра у пользователя до web/плана; `todo` instruction шага — question/form вместо explore, сводка в контексте → не второй web.

- [16:45] `total.init`: убран копипаст `one.label` на родителя — explore больше не становится «Web: …» в шапке.

- [16:27] `site.init`: `n` вынесен из `try` — в `catch` при частичном успехе больше нет `n is not defined` (ломало web после первого удачного site).

- [15:37] `site`/`total`: последний упавший site больше не красит весь web/explore («страница недоступна» при живой сводке). `box.error` — только пока нет ни одного успеха; `total` при сводке снимает `error`/`state`.

- [15:18] `web.init`: промпт вариантов цитирует запрос пользователя («новости» ≠ «рынок недвижимости» у gemma 4b), сам запрос — всегда страховочный вариант в конце. `site.prompt` — медиа вернулись: относящиеся к теме `![подпись](url)` и видео-ссылки из хвостов [images]/[video] дампа.

- [15:03] `web.init`: модель не дала вариантов запроса (пустой silent) — запросом становится текст последнего prompt пользователя («новости» → поиск «новости»), error только если и он пуст.

- [14:57] Прогон «новости» (gemma): 1) промпт вариантов запроса — имя/профиль/группа не тема («перепелки» из фамилии Перепелкин); 2) `planning.inject` — факты добыты → answer, не план; 3) успешный `site` снимает `box.error` от прежних неудачных — web с 6/8 сайтами больше не «ошибка» (и `total` не считал его error-веткой на explore).

- [14:53] `_box_context`: `error: true` снова в обычном контексте (меню/answer видят «страница недоступна»); в `total` (`evidence: false`) по-прежнему нет — без ложного провенанса в сводке.

- [14:50] Меню: пустой `content` → `thinking`, если ещё в оставшихся; иначе `next[0]` среди оставшихся после `using_blocks` (не обрыв в `chat.done`).

- [14:43] Меню: пустой `content` снова → `thinking`, если он в `next` (не `chat.done`, не `next[0]`). Silent `effort: 'off'` оставлен.

- [14:30] Silent без CoT: меню/шапка — `effort: 'off'` (`think: false`), reasoning-чанки отбрасываем, `maxOutput` 64. Нет слова в `content` — один повтор меню; снова пусто — нет шага (`chat.done`), без `next[0]`/`thinking`-фолбэка. `activation.inject` — только do (файлы/execute), не просмотр сайта; в `plan.next` ушёл с первого места.

- [14:11] Слот `reasoning` вырезаем из бокса, куда пушили — не из `_active_box()` после закрытия этапа (карточка «Рассуждаю» зависала). `total`: одни error-источники — `box.error` + текст ошибки в шапку, без LLM. `site` при сбое сразу красит `web.state` («страница недоступна»), а не прячет заход внутри ложной сводки «инструментов нет».

- [13:56] `reasoning` не храним: слот на время стрима, по концу вырезаем. Меню: пустой `content` больше не форсит `thinking` мимо `using_blocks` (петля «Думаю» на голом URL) — фолбэк только если `thinking` ещё в `next`, иначе первый оставшийся.

- [13:47] Узел `reasoning` (`ignore`): CoT стримится в свой блок и остаётся в ленте; контекст, меню, `using_blocks`, док его не видят. Меню — только `content` (английское «question» в think больше не ловит `question`). Промах → обычный `thinking`. `answer`/`question` inject: после фактов — ответить, не вежливое «нужно ещё?».

- [13:27] Меню: reasoning больше не становится `content` (silent не получает абзац think вместо EXPLORE). Промах — ищем id в тексте/`reasoning`; не нашли — блок `thinking` и auto-loop, не `answer`+stop. Silent `maxOutput` 256.

- [13:19] `answer.init` — `doc: true` только если предыдущий сосед закрытый бокс (`box` + `content` + не `error`). Сводка после explore в док; привет и ответ после thinking — нет. Цикл `class.js` не трогали.

- [12:50] Qwen «как на максимуме» при effort off: в запрос больше не кладётся `maxTokens` контекста (262k → `max_tokens` 128k). Ответ — `maxOutput` (по умолчанию 4k, silent/меню — 64). `reasoning` стримится в ленту, в `content` блока не пишется.

- [12:42] URL из промпта: `web.init` — если в последнем prompt есть ссылка, `sites` из неё, поиск не зовётся. `site.init` — неоткрытый URL из последнего prompt важнее `sites[i]` (кейс Instagram → Википедия «Балабанов»).

- [12:38] Карта узлов в `context()` из `pipe` на каждый ход: `[задача]` / `[далее]` собираются из `next` + `inject`, без хардкода имён — pipe пополнился, модель видит новые топики сама. Меню без «если не подходит — просто отвечай»: эта фраза рождала `answer` «нет поисковика» внутри уже найденного web.

- [12:02] `on_save.resolvePlace` — в кэш только успешный город. Промах (403/таймаут) больше не залипает на процесс: иначе system снова без «Рязань», модель ищет Москву. User-Agent по правилам Nominatim + таймаут 8с.

- [10:40] `_box_context` пропускает `b.error`: ошибочные site (fetch failed / 403) оставались в ленте, но их `content` — заголовок-ссылка + текст ошибки — шёл в сводку web, и модель ставила Gismeteo/Meteofor в «Источники». В док такие блоки уже не пускали; в контекст — теперь тоже нет. Листья `expand` с `error` тоже пропускаются.

- [10:30] Прогон «погода» с Рязанью: город и остановка цикла сработали, но answer отрёкся от честных данных («я всё выдумал») — итог `explore` потерял провенанс (мета-отчёт «источников не потребовалось» без единой ссылки). Три правки: 1) `total.init` — одиночный блок-данные (`role: 'user'`, `content`, не `error`) проталкивается в `box.content` без LLM-пересказа (site→web→explore схлопывается до первоисточника со ссылкой), несколько — сводка как раньше; 2) `web.prompt` — обязательный раздел «Источники», `explore.prompt` — только факты с источниками, без описания процесса/режимов; 3) `dockReports` — дедуп по `content` (проталкивание делает контент web/explore идентичным).
- [10:15] По прогону «какая завтра погода» (Qwen искал Москву вместо Рязани, rambler «загружен» с пустым телом, отчёты о футерах). 1) `on_save` — обратное геокодирование: Nominatim (OSM, без ключа, User-Agent + кэш по координатам до 2 знаков, таймаут 5с) → «Расположение: Рязань, … (lat…, lon…, tz…)»; сбой сети — голые координаты, как раньше. 2) `site.init` — выжимка короче 40 значимых символов = `error: true` (JS-рендер/антибот), `state: 'загружен'` теперь только после успешной выжимки. 3) `site.prompt` — только данные по теме задачи, устройство сайта (навигация/футер/темы/SEO) явно запрещено: раньше один сайт стоил до 3.6k токенов мусора в контексте.
- [18:15] `views.js` — два бага живой сессии (после перезагрузки всё работало, в процессе — нет). 1) Стики: офсеты считают DOM-обходы `todoView`/`prevPrompt`, Реактор кэширует их навсегда — план, появившийся посреди сессии, в кэш не попадал, шапки наезжали друг на друга. Лечение: `layoutTick` на топ-ленте, attach/detach любого view бампает, обходы читают. 2) Автоскролл: отключение follow слушало `scroll`, но при плотном стриме окно `_ignoreScroll` было открыто почти постоянно и глотало события пользователя — отмотать вверх было нельзя. Лечение: намерение пользователя только из событий ввода (`wheel` вверх / `touchmove` / drag скроллбара), `scroll` лишь возвращает follow в хвосте; `_ignoreScroll` удалён.
- [17:33] `web.init` — варианты и каскад: модель даёт до 3 вариантов запроса (парсер `searchQueries` — по строке, без кавычек/нумерации, ≤120 симв.), на каждый вариант race по `web.services` = DuckDuckGo | Yandex | SearXNG (`searchRace` на `Promise.any`: успех = непустые `results`, error/пустота проигрывают, ненастроенные сервисы выбывают сами). Победитель пишет `state: 'найдено: <source>'`. Все мимо — один error-блок со списком запросов (не 3 блока в ленте), потолок повторов остаётся. `fetch_url` для `site` — по-прежнему DuckDuckGo.

## В работе
- DuckDuckGo подозрительно пуст даже на здравые запросы — каскад обходит через SearXNG (`https://search.odant.org`); Yandex заработает при ключе.

## Ключевые решения
- Решение: race сервисов вместо последовательного каскада. Причина: одинаковый контракт `search` у всех трёх, параллель дешевле по времени, ненастроенные проигрывают гонку без спецобработки.
- Решение: перебор вариантов запроса внутри одного web-блока. Причина: переформулировка через меню = новый блок + новый silent-вызов + шум в ленте.

## Блокеры / Открытые вопросы
- Yandex (`apiKey`+`folderId`) не настроен. SearXNG — `baseUrl: https://search.odant.org` (race с DDG).
