# Прогресс: $task

## Последние изменения
- [01:08] `site.init` берёт сервис с узла `web.service`, не с блока-контейнера — `get_item(undefined)` отдавал корень WORK, отсюда `fetch_url is not a function`.

## В работе
- Чтение страниц после поиска DuckDuckGo

## Ключевые решения
- Путь сервиса живёт на узле `pipe.web`, в JSON блока `web` не копируется. `site` не затеняет экспорт `web`.

## Блокеры / Открытые вопросы
- —
