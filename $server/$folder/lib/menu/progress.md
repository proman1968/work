# Прогресс: item-menu

## Последние изменения
- [2026-07-20] `item-tree` с `:hide-readme="true"` — readme скрыты только в контекстном меню, не во всём приложении
- [2026-07-20] Кнопка «?» для корневого `handlers/readme.md` при `hideTops` — корень handlers скрыт, иначе справка handlers недоступна из меню

## В работе
- —

## Ключевые решения
- Отдельная кнопка в `item-menu`, а не на title popover — title относится к `$item` (классу), а readme — к `handlersRoot`

## Блокеры / Открытые вопросы
- —
