# Прогресс: item-node

## Последние изменения
- [2026-07-20] Добавлена кнопка «?» (`icons:help`) для узлов с дочерним `readme.md`; открытие через `execute` без autoRun родителя — readme как контекстная справка уровня (rules.md), а не пункт списка

## В работе
- —

## Ключевые решения
- Поиск readme через `items` / `get_item`, а не через отфильтрованный UI-список дерева — файл остаётся в данных владельца
- `@tap.stop` + ручное закрытие `item-menu` — иначе срабатывает autoRun родителя

## Блокеры / Открытые вопросы
- —
