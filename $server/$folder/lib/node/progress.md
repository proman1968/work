# Прогресс: item-node

## Последние изменения
- [16:10] «?» ищет `readme.md` в `storage_folder.items`, не в детях узла. Причина: документ класса живёт в хранилище (`$register/readme.md`); дети `/REGISTER` — счета.

## В работе
- —

## Ключевые решения
- Справка узла = `storage_folder` (класс = meta, папка = сама). Дети класса — предметная иерархия, не место документа.

## Блокеры / Открытые вопросы
- —
