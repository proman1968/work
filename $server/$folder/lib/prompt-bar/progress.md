# Прогресс: work-prompt-bar

## Последние изменения
- [10:40] Проп `isDo` → `isBuild` (`:success` на textarea). Причина: режим сессии переименован в `build`.

## В работе
- —

## Ключевые решения
- Контент попапа не снимок, а живой геттер от хоста: асинхронные значения (maxTokens) доезжают в открытый попап без переоткрытия.

## Блокеры / Открытые вопросы
- —
