# Прогресс: work-prompt-bar

## Последние изменения
- [16:47] Ctrl+V картинки из буфера → `files` (`_onPaste` + `_namePaste`); общий `_addFiles` с диалогом. Причина: скриншоты без диалога файлов.
- [16:05] Попап usage: якорь — кнопка (`e.currentTarget`), не координаты курсора — попап открывался поверх кнопки; в `lib/dialogs/popover` ветка якоря научилась направленному flip: нет места под якорем и сверху больше — открыться над ним (`top = r.top - offsetHeight`).
- [16:05] `work-usage-panel` читает `stats` живым геттером `host.usageStats` вместо слепка на момент клика: лимит из `maxTokens` модели грузится лениво (`_onEmpty` → `@maxTokens`), слепок показывал фолбэк 128k (у GigaChat Light реальный лимит 32k), теперь доехавшее значение обновляет открытый попап.

## В работе
- —

## Ключевые решения
- Контент попапа не снимок, а живой геттер от хоста: асинхронные значения (maxTokens) доезжают в открытый попап без переоткрытия.

## Блокеры / Открытые вопросы
- —
