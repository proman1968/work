# Qwen3-TTS CustomVoice

## Назначение

Класс `Qwen3-T-CustomVoice` (`$ai`) у `/MODELS/odant`. Назначение и контракт — по meta/`class.js` места.

## Устройство

- **path:** `/MODELS/odant/Qwen3-T-CustomVoice`
- **type:** `$ai`
- **id:** `Qwen3-T-CustomVoice`
- **label:** Qwen3-TTS CustomVoice

Источник истины полей — `class.js` в meta этой точки (readme не дублирует реализацию, только контракт).

## Контракт

- Поведение и API — в meta/`class.js` и методах точки.
- При существенной смене устройства — обновить этот readme.
