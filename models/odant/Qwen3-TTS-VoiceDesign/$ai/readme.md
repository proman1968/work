# Qwen-TTS VoiceDesign

## Назначение

Класс `Qwen3-TTS-VoiceDesign` (`$ai`) у `/MODELS/odant`. Назначение и контракт — по meta/`class.js` места.

## Устройство

- **path:** `/MODELS/odant/Qwen3-TTS-VoiceDesign`
- **type:** `$ai`
- **id:** `Qwen3-TTS-VoiceDesign`
- **label:** Qwen-TTS VoiceDesign

Источник истины полей — `class.js` в meta этой точки (readme не дублирует реализацию, только контракт).

## Контракт

- Поведение и API — в meta/`class.js` и методах точки.
- При существенной смене устройства — обновить этот readme.
