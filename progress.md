# Прогресс: WORK

## Последние изменения
- [15:51] Видение из обсуждений разложено по точкам: `whitepaper.md` §5.5 (тело/мозги/воля), `ai/readme.md` (класс-сервер, абстрактный процесс), `rules.md` C.6 (воля/действие). Причина: тезисы должны находиться там, где их найдут люди и агенты.
- Чинен краш «Привета»: адаптер отдавал движку объект модели вместо пути-строки (моя ошибка в плане). Плюс понятная ошибка в `parsePathSteps` при не-строке. Причина: движок резолвит сам, ему нужен путь.
- streamChat — по одной копии на слой: транспорт только в `$folder/$class/$ai/class.js` (туда же перенесён `buf`-фикс рваных SSE-чанков, мёртвый метод родителя удалён), сборка ответа только в движке `prompt/$method` (`$task._streamChat` — тонкий адаптер). Убран забытый `debugger`. Причина: 4 копии одного имени на 2 разные работы.

## В работе
- Цикл работы: предложение → обсуждение → изменение → прогон → `.task` → разбор.

## Ключевые решения
- Процесс абстрактный: устройство точки — только её `readme.md`; управление — только через API сервера.
- Мета-действия `freeze`/`review` — только явным вызовом, не из автоменю.

## Блокеры / Открытые вопросы
- `check.js` хранит знание устройства (наборы иконок, ожидания фрагментов) — на очереди чистка (предложение №6).
