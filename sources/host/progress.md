# Прогресс: sources/host

## Последние изменения
- [14:40] GET тела `$file` отдаёт поток с диска, не `load()` + Brotli q11 — TTFB больших JS больше не секунды на сжатии в памяти.

## В работе
- Нет

## Ключевые решения
- Раздача файла и чтение содержимого — разные контракты: HTTP GET → `download()` (поток, inherit как у `load`); `load()` остаётся для кода.
- Сжатие только до 256 КБ и только level/quality 4; крупнее (mermaid, mathjax) без encoder.

## Блокеры / Открытые вопросы
- Нет
